<div class="d-flex justify-content-center">
    <span>
        <a href="{{ route('faq.index', $id) }}" class="mb-2">Faqs</a>
        <a href="{{ route('album.index', $id) }}">Albums</a>
    </span>
    <span>
        <a href="{{ route('itinerary.index', $id) }}" class="mb-2">Itineraries</a>
        <a href="{{ route('review.index', $id) }}">Reviews</a>
    </span>
</div>
